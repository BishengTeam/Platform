# 认证报名前端实现检查报告

> 基准文档：`/home/bisheng/work/weMiniApp/认证报名接口流程.md`
> 检查日期：2026-06-08
> 检查范围：`src/pages/registration/*` + `src/services/*` + `src/types/*`

---

## 一、接口对接总览

基准文档定义 14 个接口，其中 13 个涉及前端，Service 层全部已有函数封装。

| # | 方法 | 路径 | 用途 | Service 函数 | 文件 | 状态 |
|---|------|------|------|-------------|------|:--:|
| 1 | GET | `/api/cert/certifications` | 认证列表 | `getCertificationList()` | zoneService.ts | ✅ |
| 2 | GET | `/api/cert/certifications/{id}` | 认证详情 | `getCertDetail()` | userService.ts | ✅ |
| 3 | GET | `/api/cert/nisp/template` | NISP 报名模板 | `getNispTemplate()` | userService.ts | ✅ |
| 4 | GET | `/api/cert/nisp/pinyin` | NISP 姓名拼音 | `getNispPinyin()` | userService.ts | ✅ |
| 5 | GET | `/api/cert/sangfor/verify-code` | 深信服验证码 | `getSangforVerifyCode()` | userService.ts | ✅ |
| 6 | POST | `/api/coupons/validate` | 核销考试券 | `validateCoupon()` | userService.ts | ✅ |
| 7 | GET | `/api/user/identity` | 实名认证状态 | `getIdentityStatus()` | authService.ts | ✅ |
| 8 | POST | `/api/user/identity` | 提交实名认证 | `submitIdentity()` | authService.ts | ✅ |
| 9 | POST | `/api/user/phone/decrypt` | 解密手机号 | `decryptPhone()` | authService.ts | ✅ |
| 10 | POST | `/api/orders` | 创建订单 | `createOrder()` | userService.ts | ✅ |
| 11 | GET | `/api/orders/{id}` | 订单详情 | `getOrderDetail()` | userService.ts | ✅ |
| 12 | GET | `/api/agreements` | 用户协议 | `getAgreements()` | userService.ts | ⚠️ |
| 13 | POST | `/api/payment/prepay` | 支付预下单 | `prepayOrder()` | userService.ts | ✅ |
| 14 | POST | `/api/payment/callback` | 微信支付回调 | — | — | N/A |

> ⚠️ #12 `getAgreements()` 后端 `/api/agreements` 端点不存在，已降级返回空数组。

---

## 二、页面逐页检查

### 2.1 `registration/index.tsx` — 认证列表页

**整体：✅ 基本完成**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取认证项目列表 | ✅ | `getCertificationList()` |
| 按 vendor 分组标签 | ✅ | H3C / 深信服 / NISP / 人社 |
| 关键词搜索 | ✅ | 名称 + 中文名模糊匹配 |
| 点击跳转对应表单 | ✅ | `VENDOR_ROUTE_MAP` 按 vendor 分发路由 |
| loading 加载态 | ✅ | 2026-06-08 修复：添加 `loading` state + `.finally()` 控制 |
| 空列表状态 | ✅ | 2026-06-08 修复：引入 `EmptyState` 组件展示空态 |

---

### 2.2 `registration/form.tsx` — H3C 报名表单

**整体：⚠️ 基本完成，缺实名认证和身份类型**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取认证详情 | ✅ | `getCertDetail(certId)` |
| 用户资料预填 | ✅ | `getUserProfile()` 自动填入姓名/手机/邮箱 |
| 姓名 / 手机 / 身份证 / 邮箱 | ✅ | 含校验 |
| 证件照上传 | ✅ | `Taro.chooseImage` + `uploadFile` |
| 学历 / 机构 / 考试日期 | ✅ | |
| 性别 / 国家 / 语言 切换 | ✅ | male/female, 中国/其他, 中文/English |
| 姓 / 名（拼音） | ✅ | |
| 价格明细 | ✅ | PriceRow 组件展示 |
| 创建订单 | ✅ | `createOrder()` → 跳转 confirm |
| **实名认证检查** | ✅ | 2026-06-08 修复：引入 `useIdentityCheck` hook，页面进入时自动检查认证状态 |
| **微信手机号解密** | ✅ | 2026-06-08 修复：引入 `usePhoneDecrypt` hook + TaroButton 一键授权 |
| **身份类型选择** | ✅ | 2026-06-08 修复：新增 personal/enterprise 双态切换 toggle，写入 extra_data |

---

### 2.3 `registration/form-sangfor.tsx` — 深信服报名表单

**整体：⚠️ 验证码和考试券未接入 API**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取认证详情 | ✅ | |
| 用户资料预填 | ✅ | |
| 姓名 / 手机 / 身份证 | ✅ | |
| 邮寄地址 / 机构 / 考试方向 | ✅ | |
| 邮箱 / 姓 / 名 | ✅ | |
| 考试日期 | ✅ | |
| 表单校验 | ✅ | |
| 创建订单 | ✅ | |
| **获取动态验证码** | ❌ | `getSangforVerifyCode()` 存在但未调用，验证码为纯文本输入框 |
| **核销考试券** | ❌ | `validateCoupon()` 和 `getSangforCoupons()` 均未集成 |
| **实名认证检查** | ✅ | 2026-06-08 修复：引入 `useIdentityCheck` hook |

**当前验证码区域代码：**
```tsx
<View className={styles.linkRow}>
  <Text className={styles.linkText}>{'请从深信服官方渠道获取动态验证码'}</Text>
</View>
<FormInput label={STRINGS.FORM_VERIFICATION_CODE} required ... />
```
期望行为：调用 `getSangforVerifyCode()` 获取验证码，并展示倒计时/重新获取。

---

### 2.4 `registration/form-nisp.tsx` — NISP 报名表单

**整体：⚠️ 拼音和模板下载未走 API**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取认证详情 | ✅ | |
| 用户资料预填 | ✅ | 含 school / major |
| 姓名 / 手机 / 身份证 / 邮箱 | ✅ | |
| 学校 / 专业 / 省份 | ✅ | |
| 一级 / 二级 切换 | ✅ | 二级展开额外字段 |
| 性别 / 年龄 / 学历 / 地址 / 邮编 | ✅ | 仅二级展示 |
| 照片 / 证件照 / 学信网报告 上传 | ✅ | |
| 模板下载（二级） | ⚠️ | 硬编码 URL `/api/nisp/template/download`，未调 `getNispTemplate()` |
| 模板上传（二级） | ✅ | |
| 表单校验 | ✅ | |
| 创建订单 | ✅ | |
| **拼音转换** | ❌ | 使用本地 `autoPinyin()` 占位实现，输出 `XX XX`，未调 `getNispPinyin()` |
| **实名认证检查** | ✅ | 2026-06-08 修复：引入 `useIdentityCheck` hook |

**拼音占位代码**
```ts
export function autoPinyin(name: string): string {
  return name.split('').map(() => 'XX').join(' ')
}
```

---

### 2.5 `registration/form-renshe.tsx` — 人社报名表单

**整体：⚠️ 结构最简，缺用户预填**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取认证详情 | ✅ | |
| 分支选择 | ✅ | 网络安全 / 商务数据 / AI 工程 / IoT 工程 |
| 姓名 / 手机 / 身份证 | ✅ | |
| 表单校验 | ✅ | |
| 创建订单 | ✅ | |
| **用户资料预填** | ❌ | 未调用 `getUserProfile()`，其他三个表单均有 |
| **实名认证检查** | ✅ | 2026-06-08 修复：引入 `useIdentityCheck` hook |

---

### 2.6 `registration/confirm.tsx` — 确认支付页

**整体：✅ 基本完成**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 获取订单详情 | ✅ | `getOrderDetail(orderId)` |
| 30 分钟倒计时 | ✅ | 倒计时归零禁用支付 |
| 认证名称 + 价格展示 | ✅ | |
| 协议勾选 | ✅ | `AgreementCheckbox` 组件 |
| 微信支付预下单 | ✅ | `prepayOrder()` → `Taro.requestPayment()` |
| 支付结果跳转 | ✅ | success / fail 分支 |
| **动态用户协议** | ⚠️ | `getAgreements()` 返回空数组，协议为静态文案 |
| **优惠券核销** | ❌ | 未集成 `validateCoupon()` |

---

### 2.7 `registration/xuexin-guide.tsx` — 学信网引导页

**整体：✅ 完成**

| 功能点 | 状态 | 说明 |
|--------|:--:|------|
| 五步引导流程 | ✅ | 图文步骤 |
| 验证码输入 | ✅ | 存到 `Taro.setStorageSync` |

---

## 三、缺失项汇总

### P0 — 阻断线上可用

| # | 缺失项 | 涉及文件 | 影响 |
|---|--------|---------|------|
| 1 | **NISP 拼音未调 API** | `form-nisp.tsx` L63 | `autoPinyin()` 返回 `XX XX` 占位，线上拼音字段无意义 |
| 2 | **NISP 模板下载未调 API** | `form-nisp.tsx` L198 | 硬编码 URL `/api/nisp/template/download`，与 `getNispTemplate()` 不一致 |
| 3 | **深信服验证码未调 API** | `form-sangfor.tsx` L130 | 用户手动输入，无「获取验证码」交互和下发机制 |

### P1 — 影响核心业务流程

| # | 缺失项 | 涉及文件 | 影响 |
|---|--------|---------|------|
| 4 | ~~**实名认证流程未集成**~~ | `form.tsx` `form-sangfor.tsx` `form-nisp.tsx` `form-renshe.tsx` | ✅ 2026-06-08 修复 |
| 5 | ~~**微信手机号解密未集成**~~ | 同上 | ✅ 2026-06-08 修复 |
| 6 | **深信服考试券核销未集成** | `form-sangfor.tsx` `confirm.tsx` | `validateCoupon()` + `getSangforCoupons()` 未接入 |
| 7 | **支付页优惠券核销** | `confirm.tsx` | 缺少券列表和核销交互 |

### P2 — 体验/完整性

| # | 缺失项 | 涉及文件 | 影响 |
|---|--------|---------|------|
| 8 | **用户协议动态加载** | `confirm.tsx` | 后端 `/api/agreements` 端点不存在，前端需对接后恢复 |
| 9 | **人社表单无用户预填** | `form-renshe.tsx` | 缺少 `getUserProfile()` |
| 10 | ~~**H3C 表单缺身份类型**~~ | `form.tsx` | ✅ 2026-06-08 修复 |
| 11 | **认证列表无 loading/空态** | `index.tsx` | 体验问题 |

---

## 四、总体评估

| 维度 | 完成度 | 说明 |
|------|:--:|------|
| Service 层函数封装 | **13/13** | 所有前端接口均有函数，`USE_MOCK` 开关可调试 |
| 页面文件存在 | **7/7** | 含 xuexin-guide |
| 主链路（列表→表单→订单→支付） | **通** | 端到端可走通 |
| 表单差异化功能 | **~60%** | 拼音/模板/验证码/考试券未接入 |
| 实名认证前置 | **100%** | 2026-06-08 修复：4 个表单已接入 `useIdentityCheck` hook |
| 手机号解密 | **100%** | 2026-06-08 修复：4 个表单已接入 `usePhoneDecrypt` hook |

**结论：前端骨架完整，主链路代码可用。P0 的 3 项（拼音 API、模板 API、验证码 API）需要接入页面；P1 的实名认证和考试券核销是合规必需流程。**
